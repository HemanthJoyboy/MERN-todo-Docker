# Read by docker-compose.yml (not by the app code) via ${VITE_API_BASE_URL}.
# Copy this to ".env" in the repo root and set it to wherever api-gateway
# will be publicly reachable, e.g. http://<server-public-ip>:4000
VITE_API_BASE_URL=http://localhost:4000
